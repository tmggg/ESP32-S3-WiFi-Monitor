# ESP32-S3 Wi-Fi 配网项目实现计划

## 目标

以当前 C6 工程为功能基线建立独立 ESP-IDF 工程，为 ESP32-S3-LCD-1.47B 开发板提供手机网页配网与 OpenWrt 状态显示，不修改 C6 工程和原厂 Demo。

## 已确定的产品行为

1. 首次启动且 NVS 中没有 Wi-Fi 配置时，自动进入配网模式。
2. 存在配置时优先连接保存的 Wi-Fi；30 秒内未成功获得 IP，自动进入配网模式。
3. 正常运行期间长按 BOOT（GPIO0）3 秒，清除已保存配置并进入配网模式。
4. 不采用“按住 BOOT 上电”，因为 GPIO0 是启动配置引脚，这样可能直接进入 ROM 下载模式。
5. 配网热点 SSID 格式为 `ESP32-S3-<完整SoftAP MAC>`。
6. 手机连接热点后，通过 Captive Portal 或 `http://192.168.4.1` 打开配置页面。
7. 页面可以扫描附近 2.4 GHz Wi-Fi，显示信号强度、信道和加密方式。
8. 用户选择网络、确认加密方式并输入密码。
9. ESP32-S3 保持 SoftAP 的同时，以 STA 身份测试目标网络。
10. 只有在获得目标网络 IP 后才把配置写入 NVS；随后延迟 5 秒关闭配网热点。

## 模块划分

| 模块 | 职责 | 状态 |
|---|---|---|
| `main.c` | NVS 初始化、启动决策和连接超时 | 已实现 |
| `wifi_manager.c` | APSTA、扫描、连接、事件处理、NVS 持久化 | 已实现 |
| `provisioning_portal.c` | HTTP 页面、JSON API、Captive DNS | 已实现 |
| `boot_button.c` | 运行时长按 BOOT 清除配置 | 已实现 |
| `Kconfig.projbuild` | 热点前缀、密码、超时和按键参数 | 已实现 |

## Captive Portal 系统弹窗

- DHCP Option 114 公布 `http://192.168.4.1/`。
- Captive DNS 将联网探测域名解析到设备。
- 未知 GET/HEAD 探测请求重定向到配置页。
- 弹窗由手机系统决定；保留手动访问 HTTP 地址作为后备入口。

## OpenWrt 状态显示扩展

- 基于原厂 ESP32-S3-LCD-1.47B-Test 的 SPI3、定制 ST7789T 驱动和 LVGL 8.3 配置。
- LCD 引脚为 SCLK GPIO40、MOSI GPIO45、CS GPIO42、DC GPIO41、RST GPIO39、BL GPIO46；板载 RGB LED 使用 GPIO38。
- LCD 采用 320×172 横向布局，显示 CPU、内存、温度、运行时间和 WAN 流量。
- 配网页面可指定监控设备 IPv4 地址；留空时从 DHCP 自动读取默认网关。
- OpenWrt CGI 采用 1 秒采样窗口，ESP32 在每次响应后立即发起下一次请求，实现约 1 秒一次刷新。
- Token 与 Wi-Fi 凭据一起保存到 NVS，配网页面负责录入。
- 速度自动切换 B/s、KB/s、MB/s，累计量自动切换 B、KB、MB、GB、TB。

## HTTP API

- `GET /`：配网页面。
- `GET /api/scan`：返回附近 Wi-Fi JSON 数组。
- `POST /api/connect`：提交 `ssid`、`password` 和 `auth`。
- `GET /api/status`：查询连接状态和获得的 IP。
- 其他 GET 请求：重定向至 `http://192.168.4.1/`。

## 验证计划

1. 在已加载 ESP-IDF 环境的终端执行编译。
2. 擦除 NVS 后启动，确认自动出现以 MAC 命名的热点。
3. 分别使用 Android 和 iOS 验证 Captive Portal 弹出。
4. 验证扫描结果、开放网络、WPA2 和 WPA3 网络。
5. 输入错误密码，确认热点不会关闭且页面能继续配置。
6. 输入正确密码，确认获得 IP 后写入 NVS并关闭热点。
7. 重启，确认自动连接保存的网络。
8. 正常运行时长按 BOOT 3 秒，确认配置被清除并重新出现热点。
9. 目标 AP 断电后重启设备，确认超时后自动回到配网模式。

## 后续增强项

- 在板载 LCD 上显示热点名称、密码、目标网络和连接结果。
- 对热点密码采用每台设备唯一的派生值，而不是统一默认值。
- 增加配置超时、恢复出厂设置确认和 OTA 升级入口。
- 增加自动化测试及连接失败原因的友好中文映射。

## 当前验证结果

- ESP-IDF：v5.5.4。
- 编译目标：ESP32-S3。
- 状态：已完成首轮 S3 硬件移植，按用户要求暂未编译。
- 尚未执行：连接实体开发板后的手机端与无线网络实测。
- 配网热点密码调整为 `12345678`；WPA2 协议不允许使用六位密码 `123456`。
- Wi-Fi 扫描已改为事件驱动的异步扫描，加入互斥、15 秒超时以及具体错误码回传。
- 已修复配网页面扫描按钮 ID 与 JavaScript `scan()` 函数同名、导致点击后请求未发出的前端问题。
- 针对手机 Captive Portal 请求的 `431 Request Header Fields Too Large`，HTTP 请求头上限由 1024 提升到 4096 字节，URI 上限提升到 1024 字节，清理缓冲区提升到 256 字节。
- 扫描按钮改用 `addEventListener` 绑定，避免部分手机内置浏览器对内联 `onclick` 的兼容或安全策略问题。
