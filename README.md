# ESP32-S3 Wi-Fi Monitor

ESP32-S3-LCD-1.47B 的独立 ESP-IDF 网页配网与 OpenWrt 状态显示工程。该目录从现有 C6 工程复制并按 S3 原厂 Demo 的硬件定义独立移植，原 C6 工程不受影响。

## 默认行为

- 无已保存配置或连接保存网络超过 30 秒：启动配网热点。
- 热点名称：`ESP32-S3-<SoftAP MAC>`。
- 默认热点密码：`12345678`。
- 配网页面：`http://192.168.4.1`。
- 运行期间短按 BOOT：按“默认仪表盘 → 接口1 → 接口2 → … → 默认仪表盘”循环切换。
- 运行期间长按 BOOT 3 秒：清除配置并重新进入配网模式。
- 目标 Wi-Fi 连接成功并获得 IP 后才保存配置，5 秒后关闭热点。

> 不要按住 BOOT 再上电。ESP32-S3 的 GPIO0 是启动配置引脚，这样可能进入下载模式而不会运行应用。设备正常运行后可长按 BOOT 3 秒清除配置。

## 编译和烧录

请从 ESP-IDF Terminal 执行：

```powershell
cd "D:\Projects\Linux\OpenWRT Monitor\ESP32-S3-WiFi-Monitor"
idf.py build
idf.py -p COMx flash monitor
```

原 `build` 缓存保存在 `build-pre-rename`，因为其中包含重命名前的绝对路径。首次运行 `idf.py build` 会重新生成 `build`。现有 `sdkconfig` 已指定 `esp32s3`，通常无需再运行 `set-target`。

将 `COMx` 替换为开发板实际串口。

可通过 `idf.py menuconfig` → `Wi-Fi provisioning` 修改热点名称前缀、热点密码、连接超时和长按时间。

## 使用方法

1. 首次启动设备。
2. 手机连接 `ESP32-S3-XXXXXXXXXXXX`，密码为 `12345678`。
3. 如果配置页未自动弹出，浏览器访问 `http://192.168.4.1`。
4. 点击“扫描附近 Wi-Fi”。
5. 选择目标网络并输入密码。
6. 点击“连接并保存”，等待成功提示。

ESP32-S3 仅扫描和连接 2.4 GHz Wi-Fi；纯 5 GHz SSID 不会出现。

> WPA2-PSK 密码最少为 8 个字符，因此不能把受保护热点密码设置为六位的 `123456`。这里使用最接近的 `12345678`；若必须显示为无需密码的热点，只能改为开放认证。

## 手机系统配置弹窗

固件通过 DHCP Option 114 公布配置页地址，并利用本地 DNS 和 HTTP 重定向
响应 Android、iOS 与 Windows 的联网探测。手机通常会显示“登录到网络”或
“此网络需要认证”，点击该系统通知即可进入配置页。

弹窗由手机操作系统控制，热点端无法保证每次强制显示。若未弹出，请暂时关闭
VPN、私人 DNS 和移动数据后忘记该网络并重新连接，或在浏览器手动打开
`http://192.168.4.1/`。必须使用 HTTP，不要使用 HTTPS。

## OpenWrt 状态显示

工程现已集成原开发板 ST7789/LVGL 显示支持，屏幕以 320×172 横向显示：CPU、
内存、温度、运行时间、WAN 实时上下行速度及累计上传下载量。速度自动使用
`B/s`、`KB/s`、`MB/s`，累计量自动使用 `B`、`KB`、`MB`、`GB`、`TB`。
CPU、MEM、TEMP、DOWNLOAD 和 UPLOAD 卡片使用带缓动过渡的液位背景，液面
绘制为连续起伏并横向流动的循环波浪；液位覆盖标题区域时标题切换为白色。
五张卡片采用错峰刷新，每次只更新一张且每张约 6 FPS，以避免同时刷新多个
Canvas 阻塞 SPI DMA。CPU、MEM 和 TEMP 分别按 100%、100% 和 100°C 满格，
DOWNLOAD 按 250 MB/s
满格，UPLOAD 按 10 MB/s 满格，超过对应上限时保持满格显示。
默认仪表盘的 DOWNLOAD、UPLOAD、累计量和液位按接口轮播显示，每 10 秒切换
到下一个有效接口；右上角同步显示当前接口名称。
后台持续记录每个网络接口的流量历史。短按 BOOT 可手动切换上下行波形监控页，
以绿色下载曲线和橙色上传曲线显示最近 120 秒数据，纵轴随峰值自动缩放。
仅当 WAN 接口上传或下载达到 `10 MB/s` 时才会自动进入波形监控页，其他接口
不参与自动触发判断；流量下降后，若不是
BOOT 手动监控模式，则自动返回默认仪表盘。
ESP32 最多为 16 个网络接口分别保存 120 点上下行环形缓冲；手动切换接口时，
波形、当前速度、平均速度、最大速度和纵轴缩放均使用该接口自己的数据。
波形页所有速度统一使用 Mbps，平均值和最大值按当前 120 点有效缓冲计算。
自动阈值页面使用 WAN 接口
对应的环形缓冲，不会混用其他接口历史。
屏幕启动 60 秒后，背光会自动降低到 50%。

板载 GPIO38 RGB LED 每秒显示一次 ESP32-S3 本机 CPU0 处理器负载：绿色为 0–24%，
蓝色为 25–49%，黄色为 50–74%，红色为 75–100%。该指示灯不代表 OpenWrt
的 CPU 使用率。LVGL 使用双 40 行 DMA 绘图缓冲，以减少动画期间的 SPI 分块
刷新次数。
LVGL 界面以约 60 Hz 调用显示处理；监控数据仍为每秒采样一次。波形采用循环
扫描模式，从左向右按 `0s` 到负时间绘制。无论当前流量是否达到显示阈值，
120 点环形缓冲都会持续记录；大流量发生时可立即显示此前的流量变化。
每次获得新采样后，最新点固定在左侧 `0s`，越旧的数据越靠右，并在约
650 ms 内从左向右重新绘制。X 轴默认显示 10 秒，数据超过 10 秒后逐秒自动
扩展，最大显示到右侧 `-120s`；采样数据本身仍保持每秒一次。
Y 轴刻度显示在右侧。

ESP32-S3 双核分工为：LVGL 动画、Canvas 更新和 SPI 显示提交固定在 Core 1；
OpenWrt HTTP 状态采集与本机 CPU 负载灯任务固定在 Core 0。LVGL 仍由单一任务
串行操作，避免跨核同时修改对象；双核主要用于隔离网络阻塞和显示调度抖动。

配网页面需要同时填写 OpenWrt `esp32-status` Token，并可选填写“监控设备
IP”。填写 IP 时请求指定设备；留空时自动把 DHCP 默认网关作为 OpenWrt
地址，并请求：

```text
http://<指定IP或默认网关>/cgi-bin/esp32-status?token=<Token>
```

OpenWrt CGI 脚本和安装说明位于 `openwrt/` 目录。

## 安全说明

- Wi-Fi 密码不会写入串口日志。
- 凭据仅在成功获得目标网络 IP 后写入 NVS。
- 量产时应通过 menuconfig 修改默认热点密码，最好为每台设备生成唯一密码。

## S3 硬件适配

- LCD 控制器：原厂 Demo 定制 ST7789T 驱动，320×172 横向逻辑分辨率。
- LCD SPI：SPI3，时钟 80 MHz，SCLK GPIO40、MOSI GPIO45、CS GPIO42、DC GPIO41、RST GPIO39。
- 背光：GPIO46，PWM 占空比随亮度正向变化。
- RGB LED：GPIO38；BOOT：GPIO0。
- Flash：16 MB；Octal PSRAM：80 MHz（沿用原厂 Demo 默认配置）。

## 当前验证状态

工程已按 ESP32-S3-LCD-1.47B 原厂 Demo 完成首轮移植。按约定未执行编译；首次编译和实体屏幕方向、颜色与背光验证由用户进行。
